This folder contains source code for some of the executables in the bin
directory (for programs written in a compiled language like FORTRAN). It
also contains the makefile needed to compile this source code.
